import Calculator from '../components/Calculator'

export default {
  title: 'Calculator/Calculator',
  component: Calculator,
  tags: ['autodocs'],
  argTypes: {
    // no recibe props directas, solo sirev como referencia visual ya queusa hooks internamente
  }
}

export const Default = {
  args: {}
}
